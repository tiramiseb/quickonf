name: flatpak.install
action: Install a package using flatpak
arguments:
    - Application ID of the package to install
outputs: []
example: |-
    Install "anydesk"
      flatpak.install com.anydesk.Anydesk
