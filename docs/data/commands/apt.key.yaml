name: apt.key
action: Add apt key
arguments:
    - Local name of the key (simple short text)
    - Key
outputs: []
example: |-
    NextDNS
      key = http.get.var https://repo.nextdns.io/nextdns.gpg
      apt.key nextdns <key>
